# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Local servers / build artifacts
node_modules/
.next/
dist/
build/
*.log

# Env & secrets
.env
.env.local
.env.*.local
Vercel_token
*_token
*.token
*_key.txt
*api_key*
*API_key*

# n8n-Exporte (enthalten Klartext-API-Keys) — nie committen
*.n8n.json
TRW Lead generierung.json

# CSV-Exporte (enthalten echte personenbezogene Kontaktdaten) — nie committen
*.csv

# Next.js
next-env.d.ts

# TypeScript build cache
tsconfig.tsbuildinfo

# Drizzle migrations (generated)
drizzle/
.vercel

# Reacher-Anleitung & alter n8n-Workflow (enthalten Klartext-Secrets) — nie committen
Reacher - Anleitung fuer andere Projekte.md
TRW Lg3 - find Emails.json
